import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  searchView: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
  searchIcon: {
    position: 'absolute',
    right: 0,
  },
  searchInput: {
    flex: 1,
    marginRight: 50,
  },
})
