import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  noteCircle: {
    borderWidth: 2.5,
    width: 50,
    height: 50,
    padding: 10,
    margin: 5,
    borderRadius: 50 / 2,
    alignItems: 'center',
    justifyContent: 'center',
  },
  note: {
    fontSize: 16,
  },
})
